# 变更日志

## 1.0.8 - 2026-08-21

- 内置 WXPush 微信公众号模板消息发送，直接填写 AppID、AppSecret、OpenID 和模板 ID，无需部署独立 WXPush 服务；通知内容支持寄件人、邮件标题和正文预览。
- API token 支持浏览器直开只读链接、撤销与撤销后永久删除，并新增动态授权全部账号；邮件列表和验证码链接简化为当前索引快照及最近 15 分钟窗口。
- 通知规则分类改为中文复选框，移除新建规则中的“待确认”，增加个性化邮件筛选，并合并同一邮件发往同一通道的重复投递。
- 新增相关数据库迁移、API 文档、配置教程和回归测试；账号头像始终使用邮箱首字母显示。
- 修复 API 邮件列表按邮箱地址筛选时未转换为账号公开 ID，以及长中文主题截断可能破坏 UTF-8 的问题。

## 1.0.7 - 2026-08-20

- 将新安装及未修改过的旧默认后台同步周期调整为每 5 秒；保留管理员已经选择的其他周期。
- 新增数据库迁移 16，并同步账号类型展示与相关测试。
- 账号编辑中的授权类型改为只读，避免在没有替换凭据时制造账号类型与授权方式不一致。

## 1.0.6 - 2026-08-20

- 新增统一的 Outlook 邮箱管理台默认图标，提供 SVG 源图与包含 16/32/48/64 像素的 `favicon.ico`，浏览器标签和部署后的应用均可直接使用。
- 版本资源统一升级到 `1.0.6`，保持补丁版本连续递增。

## 1.0.5 - 2026-08-20

- 修复在线升级助手继承旧 `APP_IMAGE` 和 `APP_VERSION` 环境变量，导致备份阶段仍启动旧镜像并报 `unknown command "backup-for-update"`。
- Compose 子进程现在始终以部署目录 `.env` 中的已验证镜像和版本为准，并增加环境覆盖回归测试。

## 1.0.4 - 2026-08-20

- 生产 `docker-compose.yml` 移除本地 `build` 配置，服务器重建时只能使用已发布的 GHCR 镜像，避免旧源码覆盖正式镜像。
- SPA 首页和路由回退响应增加 `Cache-Control: no-store`，升级后浏览器不会继续运行旧版前端资源。
- Vite 生产构建恢复清空输出目录，防止历史 JavaScript 资源残留并被嵌入新的 Go 二进制。
- 增加生产 Compose 和前端入口缓存回归检查，并修正数据库只读排查文档中的 SQLite 字符串引号错误。

## 1.0.3 - 2026-08-19

- 修复跨旧版本升级时由旧镜像负责备份，可能遗漏 SQLite WAL 数据并在回滚后造成账号与设置丢失的问题。
- 更新器改为先验签并拉取新镜像，再停服并由已验证的新镜像执行离线备份；备份逐表核对行数并执行完整性检查。
- 回滚改为先使用已验证的新镜像恢复数据库，成功后才切回旧镜像和旧配置。
- 恢复前对当前数据库执行 WAL checkpoint，并把恢复前数据库及其 sidecar 作为同名安全快照保留。
- 启动、更新备份和恢复均校验 `app_settings` 单例及单管理员约束，拒绝以残缺数据库进入半可用状态。

## 1.0.2 - 2026-08-19

- 单次升级在切换镜像前明确停止旧容器，并使用 `--force-recreate` 按已验证摘要重建服务。
- 健康检查失败时自动收集容器日志和 Compose 状态，终端会显示真实启动错误。
- 回滚改用独立超时上下文，主升级超时后仍可恢复旧配置、镜像和数据库。
- Docker、Cosign 等外部命令失败时保留受限长度的原始输出，便于定位生产部署问题。
- 修复更新器并发测试未等待后台任务退出导致的 Linux race 偶发失败。

## 1.0.1 - 2026-08-19

- 在线更新改为“单次升级脚本 + GitHub Release”，不再要求安装常驻 systemd 更新助手。
- Release 新增签名的 `update.sh`，一条宝塔 root 终端命令即可完成架构识别、临时 Cosign 校验、升级前备份、固定 digest 更新、健康检查和失败回滚。
- updater 新增同步 `--once` 模式，并允许使用临时 Cosign 二进制，执行完成后立即退出。
- 健康页在发现新版本时显示并复制对应仓库的一键升级命令。
- 新部署不再挂载 updater Unix Socket 或配置额外容器组权限。

## 1.0.0 - 2026-08-19

- 账号管理新增服务端搜索、25/50/100 分页、跨页全选和最多 1000 个账号的批量启用、停用与本地删除。
- 重新启用账号时保留现有 OAuth 凭据，并通过 4 并发去重队列进行后台校验，不再无条件要求重新授权。
- 统一收件箱、个性化收件箱和验证码新增安全删除，邮件只移动到 Outlook“已删除邮件”，Graph 失败不改变本地状态。
- 正式版本重构为 `1.0.0`，新增统一 `VERSION`、补丁递增脚本和连续 Release 标签校验。
- 新增四字段 OAuth 凭据导入；密码只在浏览器解析并立即丢弃。
- 新增持久化异步导入任务、4 并发验证、scope 与 Microsoft 稳定身份校验。
- 新增账号资料编辑和 OAuth 凭据安全替换。
- 新增备份删除、路径与符号链接保护、最后备份警告和审计。
- 新增 GitHub Releases 版本检查、受限 Unix Socket 更新 API和宿主机 updater。
- updater 支持固定 digest、Cosign/GitHub OIDC 验证、升级前备份、健康检查和自动回滚。
- Release manifest 在解析前验证 Cosign bundle，并与当前 `v1.0.N` 标签身份精确绑定；updater 二进制和校验和同时签名。
- updater 增加跨进程更新锁、重启中断任务恢复和带磁盘同步的原子配置写入。
- 新增 GitHub Actions 一键准备发布流程，工作流采用最小权限并将第三方 Actions 固定到提交 SHA。
- 删除侧栏构建阶段标记，首个正式版本定为 1.0.0，数据库 schema 提升到 14。
- 增加 AGPL-3.0、中文发布文档、安全策略、贡献指南和 tag 驱动发布工作流。

### 早期开发功能

- 新增统一收件箱账号筛选、独立个性化规则、清理候选全选和批处理。
- 同步周期改为秒，支持 5 秒到 60 分钟。
- 增加完整验证码 API 教程并修复邮件标签布局。

