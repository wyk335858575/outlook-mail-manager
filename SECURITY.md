# 安全策略

## 支持版本

仅最新稳定版本接收安全修复。发现漏洞后请先升级到最新正式 Release，并确认问题仍可复现。

## 私密报告漏洞

请使用 GitHub 仓库 Security Advisories 的“Report a vulnerability”私密报告入口。不要在公开 Issue、讨论区、日志附件或截图中提交 token、邮箱地址、管理员信息、数据库、服务器域名或可利用细节。

报告应包含受影响版本、部署方式、最小复现步骤、预期与实际结果、影响范围和建议修复方向。请使用脱敏测试账号和虚构数据。

## 安全边界

- 单管理员，不提供注册、邀请或多租户。
- 不收集或保存邮箱密码，不申请 `Mail.Send`，不实现发信。
- 不调用 Microsoft Graph 永久删除；清理必须可审核并有 14 天恢复期。
- OAuth、TOTP 和通知 secret 使用数据密钥加密；API token 只保存 SHA-256 哈希。
- 邮件远程图片默认阻止，HTML 必须清洗。
- 在线更新助手不接受任意命令、路径或镜像；Web 容器不得挂载 Docker Socket。

## 部署责任

公网部署必须使用 HTTPS，应用端口仅绑定回环地址。首次管理员初始化前应通过防火墙或宝塔访问限制只允许管理员 IP。数据库和主机备份属于敏感数据，必须限制读取权限并异地保存。
